import 'package:flutter/material.dart';

class ReusableGapSizedBox extends StatelessWidget {
  const ReusableGapSizedBox({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 15,
    );
  }
}
